# _canon/counts.yaml — canonical Codex numbers consolidated for cross-page consistency.
# Consumed by:
#   _scripts/check_canon_consistency.py (page-vs-canon enforced mode)
#   _scripts/build_canon_drift_report.py (snapshot + diff over time)
#
# Schema (per spec v1.3 §2.5):
#   - wrapper key: `counts` (list)
#   - per-entry required: name (str), value (number or str)

counts:
  - name: "Codex automation scripts (base)"
    value: 15
  - name: "Codex cross-link scripts (v1.3 addition)"
    value: 3
  - name: "Codex automation scripts (v1.3 total)"
    value: 18
  - name: "Codex design principles (v1.3)"
    value: 13
  - name: "CLAUDE_CONTEXT_RULES.md required Q&A rules"
    value: 4
