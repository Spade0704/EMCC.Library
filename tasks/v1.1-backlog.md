# Tool v1.1 backlog — post-v1.0 enhancement queue

## SHIPPED in S002 (2026-05-27)

- **Telegram auto-summary contract (Option A: persona instruction).**
  Persona section in `wiki.codex/git/codex/CODEX_LIBRARIAN.md` + mirror
  in `.claude/personas/CLAUDE.librarian.md` + project-root `CLAUDE.md`.
  At end of each meaningful turn, call
  `mcp__plugin_telegram_telegram__reply` with `chat_id` from
  `$TELEGRAM_CHAT_ID` for a 2-4 line summary (what done / what next /
  blockers). Soft compliance; skip trivial turns. Operator phone
  monitoring complement to existing `decision_needed` escalations
  (per `EMCC.DFDU/documents/lattice/13-TELEGRAM-INTEGRATION.md`).
  Option B (Stop hook intervention) deferred to v1.2 if compliance
  drifts; see `tasks/architect-notes.md` §S002.

---



Parking lot for enhancements deferred from v1.0 scope. Items here ship AFTER v1.0 freezes (post-P54). Source decision: chat session 05-05 polish discussion (see `tasks/sessions.md` Session 009-A).

Promotion criteria:

1. v1.0 shipped (P54 done)
2. JP signals priority (which to tackle first)
3. Item earns Goal-Driven plan + acceptance criteria → lands in `tasks/todo.md` In Progress
4. Spec amendment (if needed) ships first

⚠ = spec amendment required before build can start.

---

## Items

### B1. Backlinks dashboard

- **What:** New aggregator → `_dashboards/backlinks.md`. For each page, list pages that link to it.
- **Why deferred:** Pure addition, low risk, but expands v1.0 surface area. Ship v1.0 lean.
- **Sketch:** New script `build_backlinks.py`, agg contract per P4 precedent (run/dash_path/`__main__`). 5-field fm dashboard. Inverse scan of all `[[wikilinks]]` corpus-wide.
- **Spec impact:** Additive script (would be #16). Minor spec footnote at next rev.

### B2. "Related but unlinked" — extend `check_cross_refs.py`

- **What:** New `## Related but unlinked` section in `_dashboards/cross_refs.md`. Lists page pairs sharing canonical entities (per `_canon/roster.yaml`) but lacking mutual `[[wikilinks]]`.
- **Why deferred:** Useful but not blocking. Value scales w/ wiki size — better signal once dozens of pages exist.
- **Sketch:** Reuse roster regex + page corpus walker from P16 `check_concept_coverage.py`. Add fn `_find_unlinked_relations(pages, roster)`. Default-off via `_config/cross_refs.yaml` toggle.
- **Spec impact:** Updates §2.4 #7 contract (additive); no schema change.

### B3. Post-ingest link suggest ⚠

- **What:** After ingest completes, surface pages that should be wikilinked to new/updated pages based on `_canon/roster.yaml` entity matches. User accepts/rejects per-link.
- **Why deferred + ⚠:** Touches `INGEST_PROCEDURE.md` — ships verbatim from project root, contractually stable for v1.0 trust. Modifying = procedure file v2.
- **Sketch:** Append step 4.5 to procedure between Link and Archive. Roster scan on new/updated pages → propose links → confirm → write.
- **Spec impact:** Procedure-doc revision (`INGEST_PROCEDURE.md` v2). Spec §4.3 step 4 update. Bootstrapped v1.0 wikis don't get this until they `sync_from_kit.py` against tool v1.1.

### B4. `scaffold_source.py --reingest` flag (tentative)

- **What:** Auto-detect filename collision between `_inbox/` and `_sources/raw/`; auto-invoke `delta_source_docs.py` (P13) to surface changed sections.
- **Why tentative:** P13 already covers manual re-ingest. Question is friction: how often will framework re-ingest happen? If rare, manual delta invocation suffices and B4 drops.
- **Decision needed before promotion:** drop or keep — calibrate after first 5–10 real ingests in a live wiki.
- **Spec impact:** `scaffold_source.py` contract update; minor.

---

## Out of scope for v1.1 (deferred further)

- `framework_origin` frontmatter field — spec Principle #12 reserves to spec v1.2
- Framework-export tooling automation — spec Principle #12 explicit v1.2 deferral

---

## Notes

- Order above is rough priority guess (B1 lightest → B3 heaviest). Re-rank when promoting.
- B1+B2 are pure tool adds; B3+B4 touch shipped procedures or scaffolds.
- If a v1.1 sprint absorbs B1+B2 only, that's a clean minor release. B3 likely earns its own sprint due to procedure-doc revision overhead.
