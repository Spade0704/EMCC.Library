---
title: "Delta Force — lib-wiki + lib-summary-op readiness (dir-20260614n)"
type: decision
visibility: internal
generated: false
last_updated: 2026-06-14
---

# Delta Force — EMCC.Library readiness (dir-20260614n-readiness-library, req 96acbc3a)

**DFTS:** Hard triggers fired — multi-module (engine vendored to consumers via Sync),
breaking-contract risk (shared `markdown.strip_code` + P9 resolver), modifies complex
existing logic. Soft rubric: blast radius 2, reversibility 1, novelty 1, logic complexity 2,
approach uncertainty 2 = 8. **Convened.**

**Model policy:** Fable 5 unavailable (Mythos-access gated); per "highest available" rule
all five seats + chairman ran on **Opus 4.8**.

## Framed decisions

- **A** — Bring the Library self-wiki to production-ready. P9 `check_cross_refs` reports
  50 broken wikilinks + 13 orphans, mostly false positives: ~40 path-qualified `[[folder/Page]]`
  (Style-Guide-mandated, validator resolves bare-stem only), 3 inside an HTML comment (Glossary),
  7 inside an indented code block in the VERBATIM `SEMANTIC_LINT_PROCEDURE.md`, 8/13 orphans are
  `codex/` canon docs. Options: **A1** surgical multi-fix vs **A2** exclude `codex/` from
  `iter_content_pages`.
- **B** — Harden the Librarian `summarize(source, audience)` op. Today: prose spec + an EMCC
  consumer script that is a no-op without an LLM. Proposal: a canonical stdlib engine in Library.

## Council verdict (5 advisors, anonymized A–E in transcript, revealed below)

- **Breaker (A):** A2's `pages_scanned` blast radius rewrites count assertions silently. **Critical
  catch:** naive last-segment-stem resolution turns false-positives into **false-negatives**
  (two `Setup.md` in different folders collapse; `[[ghost/Project-Overview]]` matches a real stem
  in another dir; NTFS-vs-Linux case). Fix: resolve the **full relative path first**, fall back to
  stem only when path-qualified maps to a real page; case-sensitive. Indented-code stripping
  misfires on list continuations → real broken links go unreported. Extractive summarizer: sentence
  splitting shatters `e.g.`/`v1.3`/decimals; jargon-strip can delete the subject; demand a
  faithfulness guard (every output sentence is a substring of input) + determinism.
- **Simplifier (B):** Reject A2 (sledgehammer). Real minimum: last-segment resolution + strip HTML
  comments + small allow/curate list. **Skip indented-block parsing.** For B: the extractive engine
  is "pseudo-NLP cosplay" that overclaims — ship the **seam**, deterministic lead/truncation
  fallback, not a fake plain-language transformer.
- **Surgeon (C):** A1 in-scope; A2 architectural overreach (Library-specific `codex/` pollutes a
  generic engine + blast radius). `strip_code` edits must be **justified by a named false-positive +
  full-suite re-verify**, else behavior-laundering. Glossary curation = the real anti-gaming work.
  B: scope to the minimal canonical module, not a greenfield framework.
- **Reviewer (D):** A1, decisively (A2 hides the resolver's shape; next canon folder silently
  re-breaks). **The name is the bug for B:** an extractive sentence-picker called "plain-language
  summarizer" overclaims in the module whose canon forbids it. Split claim from mechanism — public
  facade `summarize(...)` whose docstring states plain-language is met ONLY with an injected
  `summarize_fn`; deterministic path named/documented as extractive (faithful, jargon-preserving).
  `entrepreneur` does filtering, not translation — say so. Put the "why" on the doc (`allow_orphan`
  frontmatter) and in resolver/strip_code comments.
- **Pragmatist (E):** A1. Sequence by blast radius: data edits → `strip_code` (TDD, isolate,
  full-suite after) → resolution fallback (near-zero regression surface). B: minimal stdlib
  extractive (frequency/position, no numpy/TextRank) + `NotImplementedError` LLM seam. Honest
  scores: lib-wiki 45→**70**, lib-summary-op 55→**68** (capped: no LLM wired).

## Chairman synthesis (Opus 4.8)

**Agreement (5/5):** Take **A1, reject A2.** A2 pollutes a generic engine with a Library-specific
folder name and ripples through every validator. For **B**, ship an **honest seam**, not a
capability claim — the deterministic path is extractive and must be named/documented as such; the
"plain-language" contract attaches to the injected `summarize_fn`.

**Clash + resolution — indented-code stripping (the 7):** Simplifier/Surgeon/Breaker warn against
touching shared `strip_code` for indented blocks (list-continuation false-negatives). But the 7
links live in a clean indented code block preceded by a paragraph + blank line, and leaving them
keeps the validator non-green. Chairman override **with the blind spot addressed**: extend
`strip_code` to strip indented code blocks **CommonMark-correctly** — a run of ≥4-space lines that
(a) follows a blank line and (b) is **not** a list-item continuation (preceding non-blank line is
not a list marker). This satisfies Surgeon's gate (named false-positive + full-suite re-verify) and
the Breaker's exact case gets a dedicated regression test (a `[[Link]]` in an indented list
continuation must STILL be validated).

**Clash + resolution — the 8 orphans:** prefer **real coverage over suppression** (the directive's
explicit ask). Link the 8 `codex/` canon docs from a "Protocol-canon documents" section rather than
blanket `allow_orphan`. (CODEX_LIBRARIAN's `missing_canon_sources` is a true finding — add its real
provenance sources.)

**Recommendation:**
- **A:** path-qualified resolution with **full-path verification first** (Breaker) + case-sensitive;
  `strip_code` strips HTML comments + (list-safe) indented code; curate Glossary residue; link the
  8 canon docs; add CODEX_LIBRARIAN `canon_sources`. **No A2.**
- **B:** `summarize.py` — public `summarize(source, audience, summarize_fn=None)`; deterministic
  fallback is **extractive, honestly named/documented**; audience = filtering not translation;
  faithfulness guard (output sentences ⊂ input); abbreviation-aware splitter; deterministic
  tie-breaks; LLM seam present + documented as the only path that yields true plain-language.
- **Honest scores (do not game):** lib-wiki 45→**70**, lib-summary-op 55→**68**.

**First test to write:** `test_check_cross_refs` — two `Setup.md` in different folders with
`[[a/Setup]]` must resolve to `a/Setup` and NOT be cleared by `b/Setup` (the false-negative guard).

**Integration:** this gate reviews the approach only; the independent Lattice Auditor still reviews
the actual diff post-build (framework/09 §2).
