---
title: "Delta Force — 95-push (dir-20260614ii) + mentor-wiki (dir-20260614hh)"
type: decision
visibility: internal
generated: false
last_updated: 2026-06-14
---

# Delta Force — readiness 95-push + mentor-wiki

**Directives:** `dir-20260614ii-lib-95` (lib-librarian 85→95, lib-codex 90→95; **HONEST-CAP RULE**:
real work where there's headroom, FLAG honest caps where the gap is the systemic LLM-seam/runtime —
don't game) + `dir-20260614hh-mentor-wiki` (Project-Mentor mentor-wiki 75→80; Mentor=LIVE=build).

**DFTS:** Hard triggers — modifies shared orchestrator (`update_dashboards.py`), cross-repo
(Mentor), multi-consumer (Sync). Soft rubric ≥4. **Convened.** Fable 5 unavailable → 5 seats +
chairman on Opus 4.8.

## Framed decisions
- **lib-codex 90→95:** the spec's "project-local linker plug-in" is dead (`load_plugin` runs, result
  discarded; run path is keyword-PRESENCE, `blend_results` is SCORE-based — they don't connect;
  proper wiring = a net-new scoring subsystem). Candidate real work: register the report-only
  `audit_citations.py` (built last session, standalone) into the orchestrator.
- **lib-librarian 85→95:** Cross-Project-Scan is an explicit stub blocked on EMCC orchestrator;
  curation-at-scale needs the live LLM runtime; the single-invocation lock primitive is deferred.
- **mentor-wiki 75→80 (cross-repo):** validators near-green (0 broken, 1 canon-doc orphan, rest
  clean). Candidate: orphan link ± a `cross_link_topics` see-also refresh.

## Council verdict (A–E revealed)
- **Breaker:** lib-codex wiring is fine IF scoped right — register in `SUBSCRIPTS` (its loop already
  try/excepts → failure-isolation free; the orchestrator test iterates SUBSCRIPTS dynamically), add
  a `run()` returning a **dict** (the loop assumes dict), and **do NOT add a health.md section** —
  `test_health_md_body_seven_sections_in_spec_order` asserts an exact 7-section list (8th H2 = hard
  fail + spec change). Mentor `cross_link_topics` = real 30-file cross-repo churn; run with a
  diff-gate or skip. **lib-librarian @88 hides buildable work** — the lock primitive is plain stdlib
  (atomic `O_CREAT|O_EXCL`), NOT runtime-blocked; cap only the scan, not the whole function.
- **Simplifier:** `cross_link_topics` is idempotent (no-op if the graph is unchanged) — running a
  30-page pass to justify one orphan link is disproportionate; do the one-line Home link. The
  audit_citations wiring is gold-plating; the standalone choice was correct. **Minimal honest
  delivery = 2 honest caps + 1 one-line fix.**
- **Surgeon:** lib-codex wiring = scope creep into the deliberately-fenced accuracy track + shared
  orchestrator blast radius for ~3 points — the exact "manufacture diff to chase points" the
  HONEST-CAP rule forbids. Mentor: surgical orphan link ONLY (reject the cross-repo cross-link
  rewrite). lib-librarian cap is correct.
- **Reviewer:** the deltas are inflated. A report-only stage nothing gates ≈ +1 (→91), not +3.
  lib-librarian: **a flag is not a +3 — flat-cap 85**, ceiling = runtime/orchestrator seam. Mentor
  one orphan ≈ +1 (→76). Under-using the cap rule on lib-librarian especially; the smaller honest
  number is the true one. Word each result so the number isn't read as more than it is.
- **Pragmatist:** DO the wiring (low-risk additive — `run()`+`SUBSCRIPTS`, dynamic test stays green;
  the ONE trap is the frozen 7-section test → register the stage only, no health section). lib-codex
  honest ~93. lib-librarian flat-cap 85 (genuine seam). mentor orphan link only → 78 capped; the
  `cross_link_topics` refresh would commit exactly the cosmetic churn the operator's revert-discipline
  memory forbids.

## Chairman synthesis (Opus 4.8)
**Agreement:** mentor = **orphan link ONLY**, no `cross_link_topics` (4/5: cross-repo churn,
violates the cosmetic-churn-revert discipline). lib-librarian = the gap is genuinely the
runtime/orchestrator seam.

**Clash 1 — wire audit_citations? (Breaker/Pragmatist yes, low-risk; Simplifier/Surgeon no,
gold-plating).** Resolution: **DO it**, on the safe path — `run(wiki_root)->dict` wrapper +
register in `SUBSCRIPTS` + **NO health.md section** (the 7-section contract is frozen) + own
`_dashboards/citation_audit.md` + tests. The standalone choice last session was "so it can't
red-bar the gate" — a report-only orchestrator stage doesn't red-bar (`run()` forces
`enforce=False`), so this advances integration WITHOUT reversing the intent. Real engine maturation,
but priced honestly per the Reviewer.

**Clash 2 — lib-librarian @88 vs flat-85 (Breaker's buildable lock primitive vs majority flat-cap).**
Resolution: **flat-cap 85**. The lock primitive is buildable stdlib, but it is a caller-less,
deferred-sprint item — building it now to harvest +3 is the manufacturing the rule warns against
(Surgeon/Reviewer). Log it as a real future item; do not grind it for points.

**Clash 3 — score magnitudes (Reviewer: deltas inflated).** Adopt honest, smaller numbers:
- **lib-codex 90 → 92** — real engine integration (the accuracy lint now runs in the standard
  pipeline + emits a dashboard every run), but the gap to 95 is the plugin scoring subsystem
  (future sprint) + fleet-wide dry-Sync verification (env-blocked here). Ceiling stated.
- **lib-librarian — CAPPED at 85**, ceiling = Cross-Project-Scan blocked on EMCC orchestrator +
  reliable curation/ingest AT SCALE needs the live LLM runtime (+ the deferred lock primitive).
- **mentor-wiki 75 → 77** — one orphan cleared (validators green), structural ceiling reached;
  content depth (2 R-references at gap/draft) needs source material + the live ingest runtime.

**First test to write:** `test_run_returns_dict_and_writes_dashboard` (the orchestrator contract for
the new stage). **Integration:** independent Auditor reviews the diff post-build.
