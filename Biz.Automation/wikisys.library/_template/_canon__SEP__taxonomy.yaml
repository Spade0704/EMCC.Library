# _template/_canon__SEP__taxonomy.yaml — structured classifications.
# Consumed by:
#   _scripts/check_canon_consistency.py (lookup_key = name; optional value_field = parent_name for hierarchical taxonomy)
#   _scripts/build_canon_drift_report.py (snapshot + diff over time)
#
# Schema (per spec v1.2 §2.5 + consumer-script implementation):
#   - wrapper key: `categories` (list)
#   - per-entry required keys: name (str)
#   - per-entry optional keys: parent_name (str; hierarchical-link to parent category) + project-defined extension keys
#
# Project-defined extension keys make this YAML flexible: ship classes / character
# roles / faction tiers / episode arcs / etc. Edit commented examples when project
# categories are known. Schema authority: <wiki>/04-Contributing/PROJECT_WIKI_BUILD_SPEC.md §2.5.

categories: []

# Example entries (uncomment + customize per project):
#
# - name: "<top-level-category>"
#   tier: "<tier-name>"
#
# - name: "<sub-category>"
#   parent_name: "<top-level-category>"
#   alignment: "<neutral|allied|hostile>"
